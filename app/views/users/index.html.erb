<% @users.each do |user| %>
  <div class="user">
    <%= user.email %>
    <% unless @accepted_friends.include?(user) || @pending_friends.include?(user) %>
      <%= button_to "Send Friend Request", send_friend_request_path(receiver_id: user.id), method: :post %>
    <% end %>
  </div>
<% end %>
